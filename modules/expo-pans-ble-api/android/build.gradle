apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

android {
  namespace 'expo.modules.pansbleapi'

  defaultConfig {
    versionCode 1
    versionName "0.0.0"
    consumerProguardFiles 'consumer-rules.pro'
  }
}

dependencies {
  implementation project(':expo-modules-core')
}
