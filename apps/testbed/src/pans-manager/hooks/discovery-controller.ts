import { usePansDiscovery } from "../manager-context";

/** Focused discovery controller for screens that need list, selection and controls. */
export const usePansDiscoveryController = usePansDiscovery;
